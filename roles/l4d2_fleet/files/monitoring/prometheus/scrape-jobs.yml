# Appended to prometheus.yml scrape_configs by the role.
# (The Debian prometheus package already ships a 'node' job for :9100.)
  - job_name: "l4d2"
    static_configs:
      - targets: ["localhost:9101"]
