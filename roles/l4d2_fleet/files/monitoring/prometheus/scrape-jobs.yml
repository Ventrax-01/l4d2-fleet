# Append to your prometheus.yml `scrape_configs:` (setup.sh does this automatically).
  - job_name: "node"
    static_configs:
      - targets: ["localhost:9100"]

  - job_name: "l4d2"
    static_configs:
      - targets: ["localhost:9101"]
